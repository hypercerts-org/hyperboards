import { Store } from "@/components/store";
import { Center } from "@chakra-ui/react";

const StorePage = () => {
  return (
    <Center maxWidth={968}>
      <Store />
    </Center>
  );
};

export default StorePage;
